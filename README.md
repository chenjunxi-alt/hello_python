# hello_python

我的第一个 Python 仓库。

## 项目内容

- `hello.py`：一个使用 `print()` 输出中文内容的简单程序。

## 运行方法

需要先安装 Python，然后在终端中运行：

```powershell
python hello.py
```

## 我学到的内容

- 创建 GitHub 仓库
- 在网页上编辑文件
- 使用 Commit 保存修改
- 使用 Git Clone 把仓库复制到本地
